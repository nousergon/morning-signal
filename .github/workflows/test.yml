name: CI

on:
  push:
    branches: [main, "feat/**"]
  pull_request:
    branches: [main]

concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    # Self-hosted ephemeral EC2-spot runner (morning-signal-runner-dispatcher
    # Lambda, nousergon-data infrastructure/lambdas/morning-signal-runner-dispatcher)
    # — multi-version Python tool-cache fix verified live on telos (7 rounds
    # of real bugs found+fixed, see reference_uv_python_self_hosted_runner_fix_sequence_260717).
    runs-on: [self-hosted, morning-signal-spot]
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v7

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml

      # Cross-repo dep: PyPI first; git@main fallback if a new krepis pin lands
      # before the publish workflow finishes (branch refs go stale after merge).
      - name: Pre-install krepis (PyPI or git fallback)
        run: |
          python -m pip install --upgrade pip
          pip install "krepis[anthropic,openai]>=0.11.2" || \
            pip install "krepis[anthropic,openai] @ git+https://github.com/nousergon/krepis.git@main"

      - name: Install package + dev deps
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev,google]

      - name: Run pytest with coverage
        run: pytest

      - name: Upload coverage to artifact
        if: matrix.python-version == '3.11'
        uses: actions/upload-artifact@v7
        with:
          name: coverage-report
          path: .coverage
          if-no-files-found: ignore
