[pytest]
testpaths = tests
addopts = -ra --strict-markers --cov=generate_episode --cov=feed --cov-report=term-missing --cov-fail-under=80
filterwarnings =
    ignore::DeprecationWarning:boto3.*
    ignore::DeprecationWarning:botocore.*
